# =============================================
# GGU Student Portal - Application Properties
# =============================================

# Server port
server.port=8080

# ---- MySQL Database Configuration ----
# Change username and password to match your MySQL setup
spring.datasource.url=jdbc:mysql://localhost:3306/ggu_student_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Yogesh@180
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---- JPA / Hibernate ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ---- App Name ----
spring.application.name=ggu-student-portal